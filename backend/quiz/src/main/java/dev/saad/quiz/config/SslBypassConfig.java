package dev.saad.quiz.config;

// This file is intentionally empty — SSL configuration is now handled via
// OpenAiRestClientConfig which uses Apache HttpClient 5 (scoped to the
// OpenAI HTTP client, not a global JVM mutation).
